import styled from "styled-components";
import { ToolButton } from "../../../../styles/common";

export const Container = styled.div``;

export const TopTools = styled.div``;

export const Pen = styled(ToolButton)``;

export const Eraser = styled(ToolButton)``;

export const Rect = styled(ToolButton)``;

export const Download = styled(ToolButton)`
  position: absolute;
  top: 50%;
  transform: translate(0, -50%);
`;
